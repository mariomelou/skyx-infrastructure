# SkyX IaC completion audit — 2026-09-12

This audit reflects the current repository and the read-only AWS Console evidence. It deliberately distinguishes local CDK validation, observed AWS state, planned adoption, and changes actually applied.

| Goal requirement | Evidence | State |
| --- | --- | --- |
| Dedicated CDK project created and versioned | Public repository `melou-ai/skyx-infrastructure`; current `main` is published and clean | PROVADO |
| AWS inventory completed for the bounded scope | Inventory covers VPC, routes, security groups, endpoints, ECS, ECR, ALB/target groups, Aurora, Cognito, IAM, secrets, logs, Config, WAF, ACM, alarms, budgets, and Amplify | PROVADO |
| Critical resources and dependencies mapped | Typed production manifest plus observed ARNs/IDs, listeners, health checks, roles, policies, and ownership boundaries | PROVADO |
| Adoption/import strategy defined | `docs/import-plan-2026-09-12.md` defines treatment, sequence, risks, retain/rollback gates, and external ownership; the first ECR wave and exact approval gate are in `docs/adoption-wave-1-ecr-2026-09-12.md` and `docs/approval-packet-ecr-2026-09-12.md` | PROVADO as design and approval packet; no import executed |
| CDK synth works | `npm test` runs build, synth, manifest, and `verify:gates` safety verification successfully; `npm run verify:ecr` validates the opt-in ECR template; `npm run verify:observability` validates seven alarm resources with no owned SNS/ECS/ALB/RDS resources; `npm run verify:ecs-scaling` validates two Application Auto Scaling targets and policies with no ECS service/IAM role ownership; `npm run verify:waf` validates one regional COUNT Web ACL and two ALB associations with no logging/ALB ownership; `npm run verify:log-retention` validates two retained ECS log-group templates; guarded GitHub runs `34723414012`, `34723695223`, `34723892039`, `34724135711`, `34724652329`, `34724795811`, `34724921232`, and `34916258146` passed the corresponding synth/diff and safety paths | PROVADO for reference-only, ECR, observability, ECS scaling, WAF, log-retention, and gate invariants |
| Read-only invariant preserved | Automated check confirms one `AWS::CDK::Metadata` resource, zero application resources, and eight critical outputs | PROVADO |
| Authenticated AWS diff reviewed | `sts get-caller-identity --profile skyx` returned account `129346407469` and role `AWSPowerUserAccess/mario`; `cdk diff --profile skyx` completed and showed only adoption metadata, the bootstrap parameter, and inventory outputs, with no application resources, replacements, or deletions. `SkyxAdoption` is absent from CloudFormation. | PROVADO for the non-owning preview; stack creation/import remains approval-gated |
| IaC pipeline implemented | GitHub workflow has PR/main validation, fork-safe AWS preview behavior, explicit OIDC account verification, preview-only manual dispatch by default, main-only deployment dispatch, guarded ECR import inputs, opt-in observability/ECS scaling/WAF/log-retention synthesis and diff inputs, current GitHub Actions runtimes, and corrected repository-root paths. `SkyXIacPreviewRole` exists with its read-only policy; runs `34720445280`, `34721825546`, `34722446666`, `34723414012`, `34723695223`, `34723892039`, `34724135711`, and `34920789283` completed the corresponding guarded validation paths and skipped production jobs. `SkyXIacDeployRole` exists with the scoped ECR policy; GitHub `production` remains absent. | PROVADO for preview and guarded validation paths; protected production environment/secret remain pending |
| Least-privilege CI boundary documented | `docs/ci-iam-design-2026-09-12.md` separates preview/deploy from application delivery roles; `docs/deploy-role-scope-2026-09-12.md` scopes the first-wave deploy role to the dedicated ECR adoption stack and excludes secrets/migrations by default; IAM simulation verified allowed and denied actions; `npm run verify:production-gate` provides a read-only admin handoff check; `SkyxEcsPassRole` is now attached to `SkyXGitHubActionsEcrRole` with exact ECS role resources and an ECS-only condition | PROVADO; protected environment not created |
| Application deployment PassRole grant | AWS Console evidence on 2026-09-15 shows inline policy `SkyxEcsPassRole` on `SkyXGitHubActionsEcrRole`, allowing only `iam:PassRole` for `SkyXApiTaskRole` and `SkyXEcsTaskExecutionRole` when passed to `ecs-tasks.amazonaws.com` | PROVADO for the IAM grant; backend pipeline rerun remains pending |
| Cognito task-role resource scope | AWS Console evidence on 2026-09-15 shows `SkyXApiTaskRole` trusting `ecs-tasks.amazonaws.com`, retaining `cognito-idp:AdminCreateUser` and `cognito-idp:AdminGetUser`, and now limiting `Resource` to the exact SkyX pool `arn:aws:cognito-idp:us-east-1:129346407469:userpool/us-east-1_wiOmAOPbu`; the prior wildcard was replaced | PROVADO for policy scope; invitation/RBAC runtime validation remains pending |
| Application, database, auth, network, observability, and deploy boundaries documented | Inventory, pending matrix, adoption strategy, import plan, hardening plan, hardening approval packet, pipeline, CLI runbook, ECS circuit-breaker, network, TLS/ACM, Aurora, IAM/listener, and Config ownership designs | PROVADO as documentation; runtime changes, imports, and approvals remain pending |
| ECS API task-role handoff | User-supplied AWS Console evidence dated 2026-09-14 shows task definition `skyx-api:5` in `ACTIVE` status with `SkyXApiTaskRole` as the task role and `SkyXEcsTaskExecutionRole` as the execution role. After explicit approval, the ECS service was updated from revision `4` to `5`; the running task definition page exposes both roles, the service reports 1 desired/1 running task, deployment status `Success`, and the load-balancer target reports `1 Healthy / 0 Unhealthy`. | PROVADO for the ECS rollout and task-definition role wiring; the service remains externally managed and this repository did not deploy the task definition |
| Migrations remain backend-owned | No migration command is present in IaC workflow or manifest; boundary is documented | PROVADO |
| Production safety preserved | One explicitly approved ECS service rollout changed only the `skyx-api` service from task definition revision `4` to `5`, followed by one explicitly approved least-privilege policy addition to the externally managed GitHub delivery role. No ECR import, image change, endpoint change, WAF/TLS change, alarm deployment, database mutation, or IaC deployment was executed. The deploy role and an unexecuted ECR import change set remain prepared; the CloudFormation stack is only `REVIEW_IN_PROGRESS`. | PROVADO for the bounded approved changes; all other production mutations remain gated |
| Required approvals identified | Hardening, CI role activation, CloudFormation adoption, and production ownership changes are explicitly gated; GitHub environment activation is recorded as `BLOQUEADO` for the current non-admin token and `PENDENTE` overall | PROVADO |

## Current external revalidation — 2026-09-12

Read-only AWS revalidation confirms `SkyxAdoption` is absent, `SkyxEcrAdoption` remains `REVIEW_IN_PROGRESS`, and `SkyxObservability`, `SkyxEcsScaling`, `SkyxWaf`, and `SkyxLogRetention` are absent. The reviewed ECR change set remains `CREATE_COMPLETE` with exactly one `Import` action for `AWS::ECR::Repository` / `skyx-backend`; it has not executed. The live `SkyXIacDeployRole` policy contains no `CreateStack`, `UpdateStack`, `iam:PassRole`, or image-push actions. The GitHub repository currently reports `admin=false`, and the `production` environment lookup remains `404`.

## Current ECS rollout revalidation — 2026-09-14

With the correct `AdministratorAccess/mario` session in account `129346407469`, the explicitly approved ECS service update completed successfully. The service `skyx-api` now targets `skyx-api:5`; the events show the new task started, one target registered, the previous `skyx-api:4` task drained and stopped, and the service returned to steady state. The final console state is 1 desired/1 running task, deployment status `Success`, and `skyx-api:8000` at `1 Healthy / 0 Unhealthy` behind `skyx-prod-alb`. The active revision's task-definition page exposes `SkyXApiTaskRole` and `SkyXEcsTaskExecutionRole`. This was a direct ECS service rollout, not a CDK/CloudFormation deployment, and the deployment circuit breaker remains off.

The documentation update was published at commit `dfeeb9b874a37e7e5f7e630ced0532e2f2851307`; GitHub Actions run `34919854234` completed successfully with the production mutation jobs skipped.

## Current IAM PassRole revalidation — 2026-09-15

With the correct `AdministratorAccess/mario` session in account `129346407469`, the AWS Console shows `SkyXGitHubActionsEcrRole` with three inline policies: `SkyXEcrPush`, `SkyxEcsMigrationRunTask`, and `SkyxEcsPassRole`. The policy preview confirms one `iam:PassRole` statement restricted to `SkyXApiTaskRole` and `SkyXEcsTaskExecutionRole`, with `iam:PassedToService` equal to `ecs-tasks.amazonaws.com`. A current IAM policy simulation returns `allowed` for the exact role resources. No wildcard resource, `AdministratorAccess`, or generated `AWSReservedSSO` role edit was used for this grant. The authorized Cognito policy correction then replaced the task-role wildcard with the exact SkyX User Pool ARN while preserving both Cognito actions. The grant is therefore PROVADO at the IAM-policy and simulation level, but the first subsequent backend workflow attempt still returned an AWS `AccessDenied` for `iam:PassRole`; runtime effectiveness remains pending a controlled rerun and, if it repeats, CloudTrail/session-context diagnosis.

## Backend workflow attempt — 2026-09-15

Backend workflow run `34918263994` was dispatched on `main` and successfully reached the `Run database migrations` step after authenticating to ECR and building/pushing the image. It stopped at `aws ecs run-task` with `AccessDeniedException` for `iam:PassRole` on `SkyXApiTaskRole`, so no migration task or ECS rollout was proven by that run. This was an application-repository workflow attempt, not an IaC deployment or ECR import. A rerun remains a production-side-effecting action and requires explicit approval; if authorized, verify the migration task exit code, ECS service stability, active revision, and Cognito invitation/RBAC behavior afterward.

## Current component and GitHub gate revalidation — 2026-09-15

Sequential local validation passed for `verify:ecr`, `verify:observability`, `verify:ecs-scaling`, `verify:waf`, and `verify:log-retention`. These checks validate templates and ownership boundaries only; they do not deploy or import resources. `verify:production-gate` remains correctly blocked with `PRODUCTION_GATE_BLOCKED` because the `production` environment is absent or unreadable to the current token. The authenticated GitHub web session likewise shows no access to repository options, so no unprotected fallback was created.

## Handoff evidence — 2026-09-14

The technical lead supplied an AWS Console snapshot showing `skyx-api:5` as an active Fargate task definition with `SkyXApiTaskRole` assigned to the task role field and `SkyXEcsTaskExecutionRole` retained as the execution role. The previously pending rollout was later executed after explicit approval and independently revalidated at the ECS service and active task-definition surfaces. No IaC deployment or application migration was performed by this repository.

## Current exit condition

The bounded IaC preparation objective remains active: inventory, non-owning manifest, local checks, authenticated template diff, preview OIDC pipeline, first-wave ECR component, deploy role, unexecuted import change set, opt-in observability/ECS scaling/WAF/log-retention templates, and network/TLS/Aurora/IAM/Config ownership designs are verified or explicitly gated. The approved ECS rollout, scoped application-delivery `PassRole` grant, and exact Cognito policy scope are recorded as external runtime/IAM evidence, but none created IaC ownership. The next gates remain the protected GitHub environment/secret, a successful controlled backend workflow rerun and invitation/RBAC validation, exact ECR import approval, and separately reviewed hardening changes with their own diffs, rollback plans, and approvals. The existing bootstrap template asset published by the earlier change-set diff is not application ownership or deployment.
