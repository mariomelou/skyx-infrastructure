# SkyX IaC pending matrix — 2026-09-12

This matrix separates observed evidence from CDK ownership and production hardening. The current CDK stack is still a non-owning adoption manifest: it emits references and does not create or import application resources.

| Area | Current evidence | CDK / operational status | Remaining gate |
| --- | --- | --- | --- |
| Security groups, routes, and VPC endpoint | Security-group rules and route tables are inventoried; the SkyX VPC has zero endpoints. The observed S3 gateway endpoint `vpce-0be79b16f26533b68` belongs to the separate Control Tower VPC and has the default open policy (`Principal *`, `Action *`, `Resource *`). | PROVADO as read-only inventory; external endpoint is explicitly non-owning in the manifest. Detailed dependency/cost/change-set design is in `docs/network-hardening-design-2026-09-12.md`. | Obtain workload dependency evidence and private-egress decision, then prepare a separately approved resource-by-resource change set. |
| ECS autoscaling | Both services have desired/running count 1 and no autoscaling resources. | PROVADO as current state; opt-in `SkyxEcsScaling` now synthesizes only two scalable targets and two CPU target-tracking policies; guarded validation run `34723695223` passed. | Approve per-service min/max capacity, CPU targets, cooldowns, load baseline, budget, generated diff, and deployment window. |
| ECS deployment circuit breaker | `skyx-api` now uses task definition `skyx-api:5`; `skyx-frontend` remains on `skyx-frontend:1`. The API rollout completed with 1 desired/1 running task and 1 healthy target. Circuit breaker and deployment alarms remain **Turned off**. | Current runtime state PROVADO by read-only evidence; the controlled update design is in `docs/ecs-circuit-breaker-design-2026-09-12.md`. It is not enabled by CDK or runtime. | Complete service snapshot, failure rehearsal, rollback plan, and explicit approval before changing deployment behavior. Keep it separate from autoscaling, listeners, security groups, and migrations. |
| ALB listeners and rules | Both listeners are HTTP:80 default rules with no conditions, 100% forward to their target groups, target-group stickiness off, no response headers, and server header on. API health check is HTTP `/health/live`; frontend health check is HTTP `/sign-in`; both use traffic-port, 5 healthy / 2 unhealthy threshold, 5-second timeout, 30-second interval, and success code 200. Both ALBs have HTTP/2 on, 60-second idle timeout, 3,600-second client keepalive, defensive desync mitigation, X-Forwarded-For append, cross-zone balancing on, deletion protection off, and all three ALB log types off. | PROVADO at listener/rule/target-group-health/ALB-attribute level. | Decide whether protection/logging/TLS hardening is desired before import modeling. |
| WAF and ACM/TLS | WAF and WAF Classic show zero web ACLs; no ACM certificate was visible in `us-east-1`. | WAF remains AUSENTE in production; opt-in `SkyxWaf` now synthesizes one regional COUNT Web ACL with two AWS-managed rule groups and associations to the existing ALBs; guarded validation run `34723892039` passed. ACM/TLS remains unimplemented; the exact domain/DNS/certificate/listener sequence is in `docs/tls-acm-design-2026-09-12.md`. | Approve WAF observation/logging/false-positive plan and generated diff; provide canonical domains, certificate validation, HTTPS listener diff, application URL rollout, and redirects decision. |
| CloudWatch alarms | Zero alarms observed in `us-east-1`. | AUSENTE in production. The opt-in `SkyxObservability` design now synthesizes seven alarms over existing ALB, ECS, and Aurora metrics without creating SNS or application resources; guarded validation run `34723414012` passed. | Approve the SNS topic, ALB 5XX threshold, Aurora connection threshold, generated diff, and deployment window before applying. |
| AWS Config | Recorder is on, continuous, records default resource types, and shows seven-year retention/delivery settings. | PROVADO as observed; ownership is treated as Control Tower external and the boundary is documented in `docs/config-controltower-ownership-2026-09-12.md`; no recorder/rule is recreated. | Confirm Control Tower owner and managed-rule/exception ownership before any change. |
| IAM roles and policies | ECS execution-role, GitHub OIDC-role, and CodeBuild-role policies/trust are captured. CodeBuild uses AWS-managed ECR/CloudWatch policies plus an inline CodeConnections permission. The active `skyx-api:5` task definition exposes task role `SkyXApiTaskRole` and execution role `SkyXEcsTaskExecutionRole`; the approved service rollout is steady with 1 healthy load-balancer target. AWS Console evidence on 2026-09-15 shows inline `SkyxEcsPassRole` attached to `SkyXGitHubActionsEcrRole`, restricted to those two ECS roles and `ecs-tasks.amazonaws.com`. The task role trust is correct and its Cognito actions are present, but the Cognito resource remains the wildcard `arn:aws:cognito-idp:us-east-1:*:userpool/*`. | Preview/deploy role boundaries, the API task-role handoff, and the scoped deployment `iam:PassRole` grant are PROVADOS; Cognito resource scoping remains PENDENTE. Application roles remain external and no replacement is created by this repository. | Narrow the Cognito resource to the exact SkyX pool, then rerun the backend pipeline and validate migrations/deployment; any further application-role or listener change needs policy/trust diff, IAM simulation, and explicit approval. The protected GitHub environment remains a separate gate. |
| IaC preview diff | `build`, `synth`, manifest verification, authenticated `cdk diff --method template`, and GitHub preview run `34720445280` pass against account `129346407469` / `us-east-1`. The diff contains only adoption metadata, the bootstrap parameter, and inventory outputs; it shows no application resources, replacements, or deletions. `SkyxAdoption` is absent from CloudFormation. | PROVADO as a non-owning preview; no application change set was created. | Keep the stack reference-only until component-specific approval is granted; do not create the stack, import resources, or apply hardening without a reviewed change set and explicit approval. |
| CloudFormation adoption | No application resource has been imported. The resource-by-resource treatment, dependency order, and rollback gates are documented in `docs/import-plan-2026-09-12.md`. The first candidate is scoped in `docs/adoption-wave-1-ecr-2026-09-12.md`; its opt-in template and unexecuted import change set `skyx-ecr-backend-import-20260912` were validated for one `skyx-backend` repository. | PENDENTE by design; `SkyxEcrAdoption` is only `REVIEW_IN_PROGRESS` and the change set has not executed. | Review and explicitly approve the exact change set and execution window; after execution, verify stack resources and post-import diff. |
| Pipeline IAM and deployment | GitHub workflow exists with separate preview and production jobs, fork-safe preview behavior, explicit account verification, repository-root paths, documented role boundary, and preview-only manual dispatch by default. `SkyXIacPreviewRole` was created with the read-only policy; run `34720445280` passed the authenticated preview. `SkyXIacDeployRole` now exists with the first-wave policy. | Preview pipeline and deploy-role boundary are PROVADOS; protected production environment and environment secret remain unconfigured. GitHub currently has no `production` environment, and the current token has `admin=false`. | A GitHub administrator must create `production` with required reviewers and configure `AWS_IAC_DEPLOY_ROLE_ARN`; keep execution manual and approval-gated. |
| Protected GitHub environment | The activation API returned 403 because the current token has repository `push` but not `admin`; `production` is absent. The exact admin runbook is in `docs/ci-iam-design-2026-09-12.md`. | BLOQUEADO for the current session; PENDENTE overall. No unprotected fallback was created. | Authenticate as a repository administrator, run the documented environment/branch-policy/secret commands, verify protection, then continue with the approved change set. |
| Runtime hardening | Frontend uses raw HTTP ALB URLs; some environment variables are plaintext; Aurora deletion protection is off; frontend ECS log retention was not set to expire. | Observed risks are documented, not changed. The import-oriented `SkyxLogRetention` template now covers both ECS log groups with retain policies; local 30-day verification and guarded run `34724135711` pass, but no import/update was executed. TLS and Aurora gates are detailed in `docs/tls-acm-design-2026-09-12.md` and `docs/aurora-protection-design-2026-09-12.md`. | Coordinate application URL changes, secret policy, DB protection, exact retention value, and resource-by-resource log-group import/update before applying. |
| Database migrations | Backend owns migrations; no migration was run by this project. | PROVADO as boundary. | Preserve this ownership during any future ECS or database import. |

## Bottom line

The inventory points (security groups, routes, endpoint state and policy, autoscaling state, deployment circuit-breaker state, ALB listener/rule/target-group-health/attribute state, alarms, Config, WAF absence, and the three main application IAM roles) are documented as evidence. They are not yet broadly imported or managed by CloudFormation. The first adoption candidate is scoped as one ECR repository, with `skyx-backend` recommended; its unexecuted import change set is prepared and validated. Opt-in observability, ECS scaling, WAF COUNT, and import-oriented log-retention templates are prepared and validated without application, import, or production execution; network, TLS/ACM, Aurora, IAM/listener, and Config ownership designs are documented with explicit gates. The remaining work is to configure the protected GitHub environment with an administrator, obtain explicit approval for the exact import or hardening change set, and separately approve any other hardening or ownership change described in `docs/hardening-plan-2026-09-12.md`.
