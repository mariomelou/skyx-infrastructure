# SkyX IaC completion audit — 2026-09-12

This audit reflects the current repository and the read-only AWS Console evidence. It deliberately distinguishes local CDK validation, observed AWS state, planned adoption, and changes actually applied.

| Goal requirement | Evidence | State |
| --- | --- | --- |
| Dedicated CDK project created and versioned | Public repository `melou-ai/skyx-infrastructure`; current `main` is published and clean | PROVADO |
| AWS inventory completed for the bounded scope | Inventory covers VPC, routes, security groups, endpoints, ECS, ECR, ALB/target groups, Aurora, Cognito, IAM, secrets, logs, Config, WAF, ACM, alarms, budgets, and Amplify | PROVADO |
| Critical resources and dependencies mapped | Typed production manifest plus observed ARNs/IDs, listeners, health checks, roles, policies, and ownership boundaries | PROVADO |
| Adoption/import strategy defined | `docs/import-plan-2026-09-12.md` defines treatment, sequence, risks, retain/rollback gates, and external ownership; the first ECR wave and exact approval gate are in `docs/adoption-wave-1-ecr-2026-09-12.md` and `docs/approval-packet-ecr-2026-09-12.md` | PROVADO as design and approval packet; no import executed |
| CDK synth works | `npm test` runs build, synth, manifest, and `verify:gates` safety verification successfully; `npm run verify:ecr` validates the opt-in ECR template; `npm run verify:observability` validates seven alarm resources with no owned SNS/ECS/ALB/RDS resources; `npm run verify:ecs-scaling` validates two Application Auto Scaling targets and policies with no ECS service/IAM role ownership; `npm run verify:waf` validates one regional COUNT Web ACL and two ALB associations with no logging/ALB ownership; `npm run verify:log-retention` validates two retained ECS log-group templates; guarded GitHub runs `34723414012`, `34723695223`, `34723892039`, `34724135711`, `34724652329`, `34724795811`, `34724921232`, and `34916258146` passed the corresponding synth/diff and safety paths | PROVADO for reference-only, ECR, observability, ECS scaling, WAF, log-retention, and gate invariants |
| Read-only invariant preserved | Automated check confirms one `AWS::CDK::Metadata` resource, zero application resources, and eight critical outputs | PROVADO |
| Authenticated AWS diff reviewed | `sts get-caller-identity --profile skyx` returned account `129346407469` and role `AWSPowerUserAccess/mario`; `cdk diff --profile skyx` completed and showed only adoption metadata, the bootstrap parameter, and inventory outputs, with no application resources, replacements, or deletions. `SkyxAdoption` is absent from CloudFormation. | PROVADO for the non-owning preview; stack creation/import remains approval-gated |
| IaC pipeline implemented | GitHub workflow has PR/main validation, fork-safe AWS preview behavior, explicit OIDC account verification, preview-only manual dispatch by default, main-only deployment dispatch, guarded ECR import inputs, opt-in observability/ECS scaling/WAF/log-retention synthesis and diff inputs, current GitHub Actions runtimes, and corrected repository-root paths. `SkyXIacPreviewRole` exists with its read-only policy; runs `34720445280`, `34721825546`, `34722446666`, `34723414012`, `34723695223`, `34723892039`, and `34724135711` completed the corresponding guarded validation paths and skipped production jobs. `SkyXIacDeployRole` exists with the scoped ECR policy; GitHub `production` remains absent. | PROVADO for preview and guarded validation paths; protected production environment/secret remain pending |
| Least-privilege CI boundary documented | `docs/ci-iam-design-2026-09-12.md` separates preview/deploy from application delivery roles; `docs/deploy-role-scope-2026-09-12.md` scopes the first-wave deploy role to the dedicated ECR adoption stack and excludes secrets/migrations by default; IAM simulation verified allowed and denied actions; `npm run verify:production-gate` provides a read-only admin handoff check | PROVADO; preview and deploy roles created, protected environment not created |
| Application, database, auth, network, observability, and deploy boundaries documented | Inventory, pending matrix, adoption strategy, import plan, hardening plan, hardening approval packet, pipeline, CLI runbook, ECS circuit-breaker, network, TLS/ACM, Aurora, IAM/listener, and Config ownership designs | PROVADO as documentation; runtime changes, imports, and approvals remain pending |
| ECS API task-role handoff | User-supplied AWS Console evidence dated 2026-09-14 shows task definition `skyx-api:5` in `ACTIVE` status with `SkyXApiTaskRole` as the task role and `SkyXEcsTaskExecutionRole` as the execution role. Read-only service validation shows `skyx-api` still running revision `4`, with 1 desired/1 running task and 1 healthy target. | PREPARADO / AGUARDANDO ROLLOUT; revision `5` has not been applied to the service |
| Migrations remain backend-owned | No migration command is present in IaC workflow or manifest; boundary is documented | PROVADO |
| Production safety preserved | No application deploy, ECR import, image change, application-role change, endpoint change, WAF/TLS change, alarm deployment, or database mutation was executed. The deploy role and an unexecuted ECR import change set were created; the CloudFormation stack is only `REVIEW_IN_PROGRESS`. The observability component is synth-only and references an externally supplied SNS topic. The earlier change-set diff published its generated template asset to existing CDK bootstrap storage. | PROVADO for application-resource safety; role/change-set preparation recorded |
| Required approvals identified | Hardening, CI role activation, CloudFormation adoption, and production ownership changes are explicitly gated; GitHub environment activation is recorded as `BLOQUEADO` for the current non-admin token and `PENDENTE` overall | PROVADO |

## Current external revalidation — 2026-09-12

Read-only AWS revalidation confirms `SkyxAdoption` is absent, `SkyxEcrAdoption` remains `REVIEW_IN_PROGRESS`, and `SkyxObservability`, `SkyxEcsScaling`, `SkyxWaf`, and `SkyxLogRetention` are absent. The reviewed ECR change set remains `CREATE_COMPLETE` with exactly one `Import` action for `AWS::ECR::Repository` / `skyx-backend`; it has not executed. The live `SkyXIacDeployRole` policy contains no `CreateStack`, `UpdateStack`, `iam:PassRole`, or image-push actions. The GitHub repository currently reports `admin=false`, and the `production` environment lookup remains `404`.

## Handoff evidence — 2026-09-14

The technical lead supplied an AWS Console snapshot showing `skyx-api:5` as an active Fargate task definition with `SkyXApiTaskRole` assigned to the task role field and `SkyXEcsTaskExecutionRole` retained as the execution role. A subsequent read-only service check showed `skyx-api` still on revision `4`, with one desired/running task, deployment status `Success`, and one healthy load-balancer target. This is configuration evidence only; revision `5` has not been rolled out and no running task has been independently verified with the new role. No IaC deployment or application migration was performed by this repository.

## Current exit condition

The bounded IaC preparation objective is complete: inventory, non-owning manifest, local checks, authenticated template diff, preview OIDC pipeline, first-wave ECR component, deploy role, unexecuted import change set, opt-in observability/ECS scaling/WAF/log-retention templates, and network/TLS/Aurora/IAM/Config ownership designs are verified or explicitly gated. The next phase remains approval-gated: a GitHub administrator must configure the protected environment/secret, then the exact ECR or hardening change set may be executed only after explicit approval. Production hardening remains separate and requires its own design, diff, rollback plan, and approval. The existing bootstrap template asset published by the earlier change-set diff is not application ownership or deployment.
