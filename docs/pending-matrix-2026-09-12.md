# SkyX IaC pending matrix — 2026-09-12

This matrix separates observed evidence from CDK ownership and production hardening. The current CDK stack is still a non-owning adoption manifest: it emits references and does not create or import application resources.

| Area | Current evidence | CDK / operational status | Remaining gate |
| --- | --- | --- | --- |
| Security groups, routes, and VPC endpoint | Security-group rules and route tables are inventoried; the SkyX VPC has zero endpoints. The observed S3 gateway endpoint `vpce-0be79b16f26533b68` belongs to the separate Control Tower VPC and has the default open policy (`Principal *`, `Action *`, `Resource *`). | PROVADO as read-only inventory; external endpoint is explicitly non-owning in the manifest. Detailed dependency/cost/change-set design is in `docs/network-hardening-design-2026-09-12.md`. | Obtain workload dependency evidence and private-egress decision, then prepare a separately approved resource-by-resource change set. |
| ECS autoscaling | Both services have one desired/running task. | **IMPORTADO** into `SkyxEcsScaling`: API `1–4` / CPU `60%` with policy `skyx-api-cpu-60`; frontend `1–3` / CPU `60%` with policy `skyx-frontend-cpu-60`. | Validate under representative load; no protected GitHub production run has been performed yet. |
| ECS deployment circuit breaker | `skyx-api` uses task definition `skyx-api:5`; `skyx-frontend` uses `skyx-frontend:1`; both services are steady with one desired/running task. | **IMPORTADO** into `SkyxEcsCircuitBreaker`: both services have deployment circuit breaker enabled with rollback enabled and reset-on-healthy-task enabled. | Document a failure rehearsal and rollback procedure, and keep deployment alarms separate. |
| ALB listeners and rules | Both listeners are HTTP:80 default rules with no conditions, 100% forward to their target groups, target-group stickiness off, no response headers, and server header on. API health check is HTTP `/health/live`; frontend health check is HTTP `/sign-in`; both use traffic-port, 5 healthy / 2 unhealthy threshold, 5-second timeout, 30-second interval, and success code 200. Both ALBs have HTTP/2 on, 60-second idle timeout, 3,600-second client keepalive, defensive desync mitigation, X-Forwarded-For append, cross-zone balancing on, deletion protection off, and all three ALB log types off. | PROVADO at listener/rule/target-group-health/ALB-attribute level. Application roles remain external, and the intended listener behavior is HTTP-to-HTTPS after hostname/certificate inputs exist. | Provide canonical hostnames and ACM/DNS evidence, then review the listener/application URL diff before execution. |
| WAF and ACM/TLS | No ACM certificate was visible in `us-east-1`. The regional Web ACL `skyx-prod-regional-count` now exists with ARN `arn:aws:wafv2:us-east-1:129346407469:regional/webacl/skyx-prod-regional-count/5179ccf1-bc92-4347-814e-d9667a2201b2`; it is associated with `skyx-prod-alb` and `skyx-prod-frontend-alb`. Both managed rule groups are in COUNT override and WAF logging is disabled. | **IMPORTADO** into `SkyxWaf` with the Web ACL and both associations. ACM/TLS remains unimplemented. | Provide canonical domains and certificate validation, then review the HTTPS listener/application URL diff and redirect decision. |
| CloudWatch alarms | Zero alarms observed in `us-east-1`. | AUSENTE in production. The opt-in `SkyxObservability` design now synthesizes seven alarms over existing ALB, ECS, and Aurora metrics without creating SNS or application resources; guarded validation run `34723414012` passed. | Define the SNS/on-call destination and confirm ALB 5XX/Aurora connection thresholds before applying. |
| AWS Config | Recorder is on, continuous, records default resource types, and shows seven-year retention/delivery settings. | PROVADO as observed; ownership is confirmed as Control Tower external and the boundary is documented in `docs/config-controltower-ownership-2026-09-12.md`; no recorder/rule is recreated. | No SkyX mutation planned; keep this boundary and document any future Control Tower exception separately. |
| IAM roles and policies | ECS execution-role, GitHub OIDC-role, and CodeBuild-role policies/trust are captured. CodeBuild uses AWS-managed ECR/CloudWatch policies plus an inline CodeConnections permission. The active `skyx-api:5` task definition exposes task role `SkyXApiTaskRole` and execution role `SkyXEcsTaskExecutionRole`; the approved service rollout is steady with 1 healthy load-balancer target. AWS Console evidence on 2026-09-15 shows inline `SkyxEcsPassRole` attached to `SkyXGitHubActionsEcrRole`, restricted to those two ECS roles and `ecs-tasks.amazonaws.com`; current IAM simulation returns `allowed`, and the authorized backend rerun passed migrations and rollout. The task role trust is correct, its Cognito actions are present, and its resource is now the exact SkyX User Pool. | The policy attachment, scope, simulation, API task-role handoff, Cognito resource scoping, and backend deployment/migration path are PROVADOS; Cognito invitation/RBAC/tenant-isolation validation remains pending. Application roles remain external and no replacement is created by this repository. | Preserve external application roles. Any additional IAM/listener change needs an exact policy/trust/listener diff, IAM simulation, and explicit approval. The protected GitHub environment remains a separate gate. |
| IaC preview diff | `build`, `synth`, manifest verification, authenticated `cdk diff --method template`, and GitHub preview run `34720445280` pass against account `129346407469` / `us-east-1`. Component import change sets were reviewed for import-only actions and then executed. | **PROVADO** as authenticated preview plus bounded component imports; no replacement or deletion was executed. | Keep future changes component-scoped, reviewed, and approval-gated through the protected GitHub environment. |
| CloudFormation adoption | The resource-by-resource treatment, dependency order, and rollback gates are documented in `docs/import-plan-2026-09-12.md` and `docs/reconciliation-plan-2026-09-15.md`. The bounded ECR import was executed, followed by approved imports for Aurora protection, ECS scaling, ECS circuit breaker, and WAF COUNT. | **PROVADO** for ECR, Aurora, ECS scaling, ECS circuit breaker, and WAF (Web ACL plus both associations). `SkyxObservability`, `SkyxLogRetention`, listeners, network endpoints, and IAM remain outside CloudFormation ownership. | Preserve imported resources and require a reviewed, import-only/property-only change set for each future component. |
| Pipeline IAM and deployment | GitHub workflow exists with separate preview and production jobs, fork-safe preview behavior, explicit account verification, repository-root paths, documented role boundary, and preview-only manual dispatch by default. `SkyXIacPreviewRole` was created with the read-only policy; run `34720445280` passed the authenticated preview. `SkyXIacDeployRole` now exists with the first-wave policy. | Preview pipeline and deploy-role boundary are PROVADOS; protected production environment and environment secret remain unconfigured. GitHub currently has no `production` environment, and the current token has `admin=false`. | A GitHub administrator must create `production` with required reviewers and configure `AWS_IAC_DEPLOY_ROLE_ARN`; keep execution manual and approval-gated. |
| Protected GitHub environment | The activation API returned 403 because the current token has repository `push` but not `admin`; `production` is absent. The exact admin runbook is in `docs/ci-iam-design-2026-09-12.md`. | BLOQUEADO for the current session; PENDENTE overall. No unprotected fallback was created. | Authenticate as a repository administrator, run the documented environment/branch-policy/secret commands, verify protection, then continue with the approved change set. |
| Runtime hardening | Frontend still uses raw HTTP ALB URLs; some environment variables are plaintext; frontend ECS log retention was not set to expire. Aurora `skyx-prod-db` now has deletion protection enabled and 30-day backups/PITR. | **PARTIAL / APLICADO** for Aurora; TLS, secret policy, and log retention remain unchanged. The import-oriented `SkyxLogRetention` template still uses 30 days as an unconfirmed review input. | Coordinate application URL changes, secret policy, exact log-retention value, and resource-by-resource CDK adoption/update. |
| Database migrations | Backend owns migrations; no migration was run by this project. | PROVADO as boundary. | Preserve this ownership during any future ECS or database import. |

## Bottom line

The inventory points (security groups, routes, endpoint state and policy, autoscaling state, deployment circuit-breaker state, ALB listener/rule/target-group-health/attribute state, alarms, Config, WAF, and the three main application IAM roles) are documented as evidence. The bounded ECR repository, Aurora protection, ECS autoscaling, ECS circuit breaker, and WAF COUNT resources are now managed by their scoped CloudFormation stacks without replacement or deletion. The remaining work is to configure the protected GitHub environment, resolve SNS/on-call, hostname/certificate, network-egress, alarm-threshold, and retention inputs, and separately approve each hardening or ownership change described in `docs/hardening-plan-2026-09-12.md`.
